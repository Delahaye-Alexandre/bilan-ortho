# Verrouillage des dépendances directes (versions exactes, marqueurs de
# plateforme conservés) : deux builds à des mois d'écart produisent le même
# logiciel. La CI (tests + build Windows) installe CE fichier.
#
# Régénérer après un changement de requirements.txt :
#   pip install -r requirements.txt
#   pip freeze   # reporter ici les versions des dépendances directes
#
# L'extension .txt n'est pas cosmétique : Dependabot ne surveille pas un
# fichier « .lock » générique. C'est ce fichier-ci qu'il doit proposer de
# monter, puisque c'est celui qui est installé.
fastapi==0.140.0
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-multipart==0.0.32
sqlcipher3-binary==0.6.0 ; sys_platform == "linux"
sqlcipher3==0.6.2 ; sys_platform == "win32"
sqlite-vec==0.1.9
faster-whisper==1.2.1
python-docx==1.2.0
reportlab==5.0.0
pypdf==6.14.2
